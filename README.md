# OCR Application 

## Setup Instructions 
1. Install Tesseract with Amharic support 
2. Run backend: cd backend && pip install -r requirements.txt && uvicorn main:app 
3. Run web: cd web && npm install && npm start 
4. Run mobile: cd mobile && flutter pub get && flutter run 
